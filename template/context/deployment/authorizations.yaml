# 人が明示的に許可したことだけを書く。書いていないものは全部禁止。
# ここを書き換えるのは人。エージェントは読むだけで、書き換えてはいけない。

deploy:
  sandbox: denied        # allowed にすると Sandbox 型の環境へデプロイできる
  production: denied     # 常に denied のままにする。本番は人が手で行う

external_systems:        # 開発環境の実システムに触ってよいか
  # 例:
  # dev_db:    read-only     # denied | read-only | read-write
  # dev_sap:   denied

# 許可した理由と、いつまでか (監査用)
note: ""
