# context/environment/ — 実行環境の資料

Mule runtime の版、Java の版、接続先 (DB / Salesforce / SAP) の URL と認証方式、コネクタの版。
無ければ空でよい。`/mule-start` が自分で調べ、根拠つきで `resolved.md` に書く。
接続先の資格情報そのものはここに置かない (環境変数か secure properties)。
