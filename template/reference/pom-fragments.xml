<!-- pom.xml に足す断片。api/ (RAML) と samples/ (受け入れ条件) をクラスパスに乗せる。
     api/ が無いと apikit:config の api="api/customer-api.raml" が "Raml not found" になる。
     samples/ が無いと MUnit の readUrl("classpath://samples/...") が読めない。 -->
<build>
    <resources>
        <resource>
            <directory>src/main/resources</directory>
        </resource>
        <resource>
            <directory>api</directory>
            <targetPath>api</targetPath>
        </resource>
    </resources>
    <testResources>
        <testResource><directory>src/test/resources</directory></testResource>
        <testResource>
            <directory>samples</directory>
            <targetPath>samples</targetPath>
        </testResource>
    </testResources>
</build>

<!-- DB コネクタを使うときの JDBC ドライバ。**コネクタ本体とは別に、これが <dependencies> に要る。**
     無いと起動時に "Cannot load driver class" で落ちる (MUnit は db:* を mock するので緑のまま通り、
     配備してから気づくことになる)。
     **版は書いていない。推測で書かない** (このプラグインの「GAV は推測しない」規則。knowledge/gotchas.md)。
     使う製品の正しい groupId / artifactId / version を Maven Central か
     `anypoint-cli-v4 dx mule describe-connector` で確かめてから埋める。
     Derby (テスト用) / MySQL / PostgreSQL / Oracle / SQL Server で座標は全部違う。 -->
<!--
<dependencies>
    <dependency>
        <groupId>SET_DRIVER_GROUP_ID</groupId>
        <artifactId>SET_DRIVER_ARTIFACT_ID</artifactId>
        <version>SET_DRIVER_VERSION</version>
    </dependency>
</dependencies>
-->
